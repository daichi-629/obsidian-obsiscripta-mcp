# plan_stdio.md レビュー（反映版）

## 前提の反映
- 外部アクセス禁止、ローカルのみで利用する。
- tool呼び出し結果の `content` はMCP形式をそのまま中継する。
- npmは使わず、pnpmへ全面移行する。

## 指摘（重要度順）

### Low
- **`/tools` の `hash` 安定性の前提明記**: 深い正規化は入ったが、`inputSchema`内の配列順が安定である前提を明記しておくと誤解がない。`plan_stdio.md:61`
